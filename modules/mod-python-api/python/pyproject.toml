[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "acore-python-api"
version = "0.1.0"
description = "Acknowledged control of provider-owned AzerothCore bots"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["acore_api*"]
